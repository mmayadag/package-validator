# GitHub token for the GraphQL API (required)
TOKEN=ghp_your_github_token
# GITHUB_ENDPOINT=https://api.github.com/graphql
# NPM_REGISTRY=https://registry.npmjs.org

# Swagger UI and the OpenAPI document at /docs; set to false to hide the API surface
# DOCS_ENABLED=true

# SendGrid: the report is emailed only when both values are set
SENDGRID_API_KEY=
EMAIL_FROM=
EMAIL_SUBJECT=Dependency report

# Host port for the UI container (docker compose)
UI_PORT=8080
# Public address of the UI, used for unsubscribe links in emails
PUBLIC_URL=http://localhost:8080

# Local development without Docker
# PORT=3288
# LOG_LEVEL=log
# DATABASE_PATH=data/package-validator.db
# CORS_ORIGIN=http://localhost:5173
